this is the news page

<ul class="news-list">
    {{#each data}}
    <li>
        <p class="">{{this.id}}</p>
        <p class="">{{this.name}}</p>
        <p class="">{{this.date}}</p>
        <img src="{{this.featureImage}}" alt="">
    </li>
    {{/each}}
</ul>